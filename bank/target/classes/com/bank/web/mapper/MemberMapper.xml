<?xml version="1.0" encoding="UTF-8"?>

<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" 
 "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

 <mapper namespace="com.bank.web.mapper.MemberMapper">
	<select id="selectMember" resultType="com.bank.web.domain.MemberVO" parameterType="com.bank.web.domain.MemberVO">
		SELECT * 
		FROM   member 
		WHERE  userid = #{userid} 
		AND    password = #{password}
	</select> 
	<insert id="insertMember" parameterType="com.bank.web.domain.MemberVO">
		INSERT INTO member
		VALUES     (#{userid},
		            #{name}, 
		            #{password},
		            #{addr}, 
		            #{birth})
	</insert>
 	<select id="selectUserid" resultType="string" parameterType="string">
		SELECT userid
		FROM   member
		WHERE  userid = #{userid} 	
 	</select>
 	<update id="update" parameterType="com.bank.web.domain.MemberVO">
 		UPDATE member
 		SET    password = #{password},
 			   addr     = #{addr}
 		WHERE  userid   = #{userid}
 	</update>
 	<delete id="deleteMember" parameterType="string">
 		DELETE 
 		FROM  member
 		WHERE userid = #{userid}
 	</delete>
 </mapper>